# Python 工具层

Python 在本仓库中定位为实验工具层，用于构造 baseline、校验结果、整理 benchmark 数据和可视化。

## 关注范围

- PyTorch baseline
- 输入数据生成
- CUDA/Rust kernel correctness check
- benchmark 结果解析
- 图表绘制与实验报告生成

## 非目标

Python 不作为本仓库的主线实现语言，不单独展开通用 Python 学习路线。
